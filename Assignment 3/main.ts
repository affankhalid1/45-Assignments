// Declaring variable
let author : string = "John C. Maxwell";
let quote : string = "A person who never made a mistake never tried anything new.";

// Print Output
console.log(`${author} once said, "${quote}"`)