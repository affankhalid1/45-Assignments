// Declare a variable to store a persons name
let name_whitespaces = "\n\t\t\t\tAFFAN KHALID";

 // print the name with white-spaces
console.log(`Name with White-Spaces : ${name_whitespaces}`);

// Declare a variabe to Stripped name
let stripped_name = name_whitespaces.trim();

// Print a stripped name 
console.log(`Stripped name : ${stripped_name}`); 